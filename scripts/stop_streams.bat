@echo off
echo Publishers started by start_rtsp_simulator.bat stop cleanly with Ctrl+C in that console.
echo If they were started in a separate console, press Ctrl+C in that console.
echo This script intentionally does not terminate arbitrary ffmpeg.exe processes.
